# EXAMPLE ONLY - do NOT commit real values.
# Shared HS256 signing key for the internal X-MCP-Identity JWT.
# The gateway SIGNS with this key; mock-mcp-server VERIFIES with the SAME key.
# Create it via kubectl (see README) - do not apply this file.
apiVersion: v1
kind: Secret
metadata:
  name: mcp-internal-signing
  namespace: mcp-gateway
type: Opaque
stringData:
  jwt-secret: "<LONG_RANDOM_INTERNAL_JWT_SECRET>"
