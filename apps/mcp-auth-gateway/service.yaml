apiVersion: v1
kind: Service
metadata:
  name: mcp-auth-gateway
  labels:
    app: mcp-auth-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "40"
spec:
  type: ClusterIP
  selector:
    app: mcp-auth-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
