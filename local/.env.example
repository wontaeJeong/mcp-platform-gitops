# Copy to local/.env and provide nonempty local-only values.
KEYCLOAK_ADMIN_PASSWORD=
LOCAL_USER_PASSWORD=
MCP_API_TESTS_CLIENT_SECRET=
MCP_INTERNAL_JWT_SECRET=

# Absolute path to the trusted CA bundle consumed by sibling image builds.
CA_CERT_FILE=
