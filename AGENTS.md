# PROJECT KNOWLEDGE BASE

**Generated:** 2026-07-11 13:54:14 KST
**Commit:** 3e00fb4
**Branch:** main

## OVERVIEW

GitOps source for a mock MCP service behind a Keycloak-backed authentication gateway. Production uses ArgoCD and Kustomize; local development uses an opt-in Docker Compose stack plus shell/Python contract tooling.

## STRUCTURE

```text
./
├── apps/       # Four production Kustomize boundaries
├── argocd/     # App-of-apps root and child Application wiring
├── local/      # Compose topology and local-only application config
├── scripts/    # Build, lifecycle, auth, and verification entry points
└── tests/      # Executable render and behavior contracts
```

## WHERE TO LOOK

| Task | Location | Notes |
|------|----------|-------|
| Deploy the full platform | `argocd/root-application.yaml` | Root excludes itself and manages four child Applications |
| Change production workloads | `apps/*/kustomization.yaml` | Each directory is an independent render boundary |
| Change deployment order | `argocd/*-application.yaml`, `apps/keycloak/cnpg-cluster.yaml` | Waves are 10 DB, 20 Keycloak, 30 bootstrap, 40 gateway, 50 mock |
| Change realm/IdP/client setup | `apps/keycloak-bootstrap/bootstrap.sh` | Idempotent `kcadm` reconciliation run as a Sync hook |
| Change gateway auth/routing | `apps/mcp-auth-gateway/configmap.yaml` | Raw generator input stored as `config.yaml` in a generated ConfigMap |
| Change local topology | `local/compose.yaml` | Runtime env files are generated by `scripts/local-up.sh` |
| Validate production manifests | `tests/render-production.sh` | Renders every app with `kubectl kustomize` |
| Validate local contracts | `tests/local-compose.sh` | Broadest check; also runs render, source-ID, syntax, and compile checks |

## CODE MAP

Codegraph is unavailable. Python symbols come from LSP; manifest reach is established from ArgoCD paths and executable test callers.

| Symbol / entry | Type | Location | Refs | Role |
|----------------|------|----------|------|------|
| `mcp-platform-root` | ArgoCD Application | `argocd/root-application.yaml` | deploy root | Reconciles child Application manifests |
| `kustomization.yaml` | Kustomize boundary | `apps/*/kustomization.yaml` | root apps + render test | Builds each production unit |
| `bootstrap.sh` | shell entry | `apps/keycloak-bootstrap/bootstrap.sh` | Job + reconciliation test | Reconciles realm, IdP, mappers, scope, and CLI client |
| `dirty_identifier()` | Python function | `scripts/source-identifier.py:29` | build script + test | Hashes dirty sibling worktrees deterministically |
| `source_record()` | Python function | `scripts/source-identifier.py:49` | `dirty_identifier()` | Encodes tracked, deleted, symlink, mode, and gitlink state |
| `local-pkce.py:main()` | Python entry | `scripts/local-pkce.py:57` | README + contract test | Runs browser PKCE and MCP `whoami` smoke flow |
| `local-compose.sh` | shell test root | `tests/local-compose.sh` | direct | Validates local topology and chains lower-level checks |

## CONVENTIONS

- Runtime resources use namespace `mcp-gateway`; namespace and live Secrets are out-of-band prerequisites.
- `argocd/root-application.yaml` is the preferred deployment entry. Applying children individually loses cross-Application ordering.
- `REPLACE_WITH_GIT_SHA` is deliberate in gateway and mock kustomizations; external CI stamps source revisions.
- Kustomize generators own the bootstrap script and gateway configuration ConfigMaps. Edit generator inputs, not rendered names.
- Gateway signs `X-MCP-Identity` using `MCP_INTERNAL_JWT_SECRET`; mock verifies the same Secret using `MCP_IDENTITY_JWT_SECRET`.
- Contour and `star-mcp-aidev-tls` are production ingress assumptions. Gateway and Keycloak are public; mock is not.
- Local images are built from sibling repositories, pushed through `localhost:5000`, and consumed by immutable digest.
- Tests are shell executable specifications, not a framework suite; exact strings encode security and topology contracts.

## ANTI-PATTERNS (THIS PROJECT)

- Never commit live Secret values or a corporate CA bundle. Only `secrets.example.yaml` and `local/.env.example` are commit-safe templates.
- Never apply `secrets.example.yaml` or add it to Kustomize resources.
- Never bypass TLS with `curl -k` or `--insecure`; provide the real CA bundle through an absolute local path or Secret.
- Never expose `mock-mcp-server` through an Ingress or host port; gateway is the sole public MCP path.
- Never mask Keycloak bootstrap failures with `|| true`; retries are bounded and attachment failures are fatal.
- Never move MCP Inspector into Compose or give it the Docker socket/application secrets.
- Never replace digest-pinned local image references with mutable tags or Compose builds.

## UNIQUE STYLES

- Production is app-of-apps plus four Kustomize roots; there is no parent `apps/kustomization.yaml`.
- `apps/keycloak-bootstrap/bootstrap.sh` is generated into a ConfigMap and mounted into a hook Job.
- Dirty sibling worktrees receive a deterministic SHA-256 source identity; clean trees use the full Git commit SHA.
- `tests/local-compose.sh` embeds Python for structured JSON assertions and deliberately invokes other contract scripts.

## COMMANDS

```bash
# Core repository contracts
tests/bootstrap-reconciliation.sh
tests/render-production.sh
bash tests/source-identifier.sh
tests/local-compose.sh

# Production deployment entry
kubectl apply -f argocd/root-application.yaml

# Local happy path
cp local/.env.example local/.env
scripts/local-build-push.sh
scripts/local-up.sh
scripts/local-verify.sh
scripts/local-pkce.py          # interactive browser flow
scripts/local-down.sh
```

## NOTES

- No in-repo CI workflow, Helm chart, schema validator, or policy engine exists; current gates are Kustomize/Compose rendering plus assertions.
- Local build scripts require sibling `../mcp-auth-gateway` and `../mock-mcp-server` repositories.
- `scripts/local-down.sh` removes generated runtime files but intentionally preserves user-managed `local/.env`.
- Cluster-level ArgoCD, CloudNativePG, and Contour installations are assumed, not reconciled here.
